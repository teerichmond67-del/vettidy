import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function MedicationsTab() {
  return (
    <ScreenPlaceholder
      title="Medications"
      subtitle="Premium — list, dose log, per-caregiver history"
    />
  );
}
