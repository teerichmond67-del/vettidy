import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function DocumentsTab() {
  return (
    <ScreenPlaceholder
      title="Documents"
      subtitle="Vault: list, upload, status indicators, retry failed uploads"
    />
  );
}
